# EcoBot Environment Configuration
#
# EcoBot is designed to run completely out-of-the-box without any external API keys or credentials.
# The chatbot runs on a local rules-based keyword engine and database collections are file-based JSON.
#
# No environment variables (.env) are required to boot or run the application.

